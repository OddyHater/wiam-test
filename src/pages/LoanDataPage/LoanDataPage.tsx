import { LoanForm } from 'src/components/entities';
import { PageContainer } from 'src/components/shared';

export const LoanDataPage = () => {
  return (
    <PageContainer>
      <LoanForm />
    </PageContainer>
  );
};
